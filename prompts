---

## Sprint 11 — QA Automation

*1.*
"Here's Sprint 11's brief — it's testing this time, Jest + RTL, minimum 3 components, one interaction test, one mocked fetch test, 70% coverage. Walk me through what's actually required versus what's just an example before we start."

*2.*
"I want to test real components from Cine-Stream, not throwaway ones — here's the repo. Can you pull the actual components and figure out what maps well to each phase?"

*3.*
"Give me the install command and jest config to get started."

*4.*
"npm test is throwing a JSON parse error in package.json after I added the test scripts."

*5.*
"Same error again after fixing the comma — package.json still shows unsaved changes in the tab."

*6.*
"Give me the first real test — Navbar, since it's the simplest one."

*7.*
"Getting 'expect(...).toBeInTheDocument is not a function,' plus a validation warning about an unknown option called setupFilesAfterEach."

*8.*
"Navbar's green. Give me MovieCard next — it's got real props and the favorites toggle."

*9.*
"MovieCard's passing, 6/6. Now SearchBar — it uses useRouter and useSearchParams, not sure how that's supposed to work outside a real Next.js page."

*10.*
"All three components passing together, 12/12. Before Phase 3 — how do I actually submit this? What goes on GitHub, is anything deploying to Vercel, and what do I even say in the video?"

*11.*
"Let's do Phase 3 — mock the TMDB fetch layer."

*12.*
"tmdb.js tests passing too, 16/16 total. What's the coverage look like?"
